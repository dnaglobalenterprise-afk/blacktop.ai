import React from 'react';

// If you already have a real dashboard UI in here, paste it back later.
// This default export is required so App.js can import it as:
// import CommandCenter from './components/CommandCenter/Dashboard';

export default function CommandCenter(props) {
  return (
    <div style={{ padding: 24, fontFamily: 'Arial' }}>
      <h2 style={{ marginTop: 0 }}>Command Center</h2>
      <div>✅ CommandCenter is now exporting correctly.</div>
    </div>
  );
}
